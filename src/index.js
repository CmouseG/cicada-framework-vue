import { devDependencies, version } from '../package.json'

export default class Cicada {
    static version
    constructor() {
    Cicada.version = version

        this._initialize()
    }
    _initialize  () {

    }
}